<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.financedashboard.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.financedashboard.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="phone_number" property="phoneNumber" />
        <result column="avatar_url" property="avatarUrl" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <insert id="insert" parameterType="com.example.financedashboard.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_info (username, password, email, phone_number, avatar_url, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{phoneNumber}, #{avatarUrl}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE email = #{email}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE id = #{id}
    </select>

    <update id="update" parameterType="com.example.financedashboard.entity.User">
        UPDATE user_info
        SET username = #{username},
            password = #{password},
            email = #{email},
            phone_number = #{phoneNumber},
            avatar_url = #{avatarUrl},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
</mapper>