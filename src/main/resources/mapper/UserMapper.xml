<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.financedashboard.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.financedashboard.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <insert id="insert" parameterType="com.example.financedashboard.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_info (username, password, email, phone, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE email = #{email}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM user_info WHERE id = #{id}
    </select>

    <update id="update" parameterType="com.example.financedashboard.entity.User">
        UPDATE user_info
        SET username = #{username}, password = #{password}, email = #{email}, phone = #{phone}, status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>
</mapper>