<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.financedashboard.mapper.StockInfoMapper">

    <select id="findAll" resultType="com.example.financedashboard.entity.StockInfo">
        SELECT * FROM t_stock_info ORDER BY id ASC;
    </select>

</mapper>
