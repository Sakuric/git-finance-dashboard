<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.financedashboard.mapper.StockInfoMapper">
    <resultMap id="BaseResultMap" type="com.example.financedashboard.entity.StockInfo">
        <id column="id" property="id" />
        <result column="stock_code" property="stockCode" />
        <result column="stock_name" property="stockName" />
        <result column="exchange" property="exchange" />
        <result column="industry" property="industry" />
        <result column="sector" property="sector" />
        <result column="listing_date" property="listingDate" />
        <result column="current_price" property="currentPrice" />
        <result column="yesterday_close" property="yesterdayClose" />
        <result column="change_percent" property="changePercent" />
        <result column="total_market_cap" property="totalMarketCap" />
        <result column="status" property="status" />
        <result column="last_update_time" property="lastUpdateTime" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM stock_info ORDER BY id ASC
    </select>

    <select id="findByStockCode" resultMap="BaseResultMap">
        SELECT * FROM stock_info WHERE stock_code = #{stockCode}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM stock_info WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.financedashboard.entity.StockInfo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO stock_info (stock_code, stock_name, exchange, industry, sector,
                                listing_date, current_price, yesterday_close, change_percent,
                                total_market_cap, status, last_update_time, created_at, updated_at)
       VALUES (#{stockCode}, #{stockName}, #{exchange}, #{industry}, #{sector},
               #{listingDate}, #{currentPrice}, #{yesterdayClose}, #{changePercent},
               #{totalMarketCap}, #{status}, #{lastUpdateTime}, #{createdAt}, #{updatedAt})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO stock_info (stock_code, stock_name, exchange, industry, sector,
                                listing_date, current_price, yesterday_close, change_percent,
                                total_market_cap, status, last_update_time, created_at, updated_at)
       VALUES
       <foreach collection="stockInfos" item="stock" separator=",">
           (#{stock.stockCode}, #{stock.stockName}, #{stock.exchange},
            #{stock.industry}, #{stock.sector}, #{stock.listingDate}, #{stock.currentPrice},
            #{stock.yesterdayClose}, #{stock.changePercent}, #{stock.totalMarketCap}, #{stock.status},
            #{stock.lastUpdateTime}, #{stock.createdAt}, #{stock.updatedAt})
        </foreach>
    </insert>

    <insert id="batchInsertOrUpdate" parameterType="java.util.List">
        INSERT INTO stock_info (stock_code, stock_name, exchange, status, created_at, updated_at)
        VALUES
        <foreach collection="stockInfos" item="stock" separator=",">
            (#{stock.stockCode}, #{stock.stockName}, #{stock.exchange},
             #{stock.status}, #{stock.createdAt}, #{stock.updatedAt})
        </foreach>
        ON DUPLICATE KEY UPDATE
        stock_name = VALUES(stock_name),
        exchange = VALUES(exchange),
        status = VALUES(status),
        updated_at = VALUES(updated_at)
    </insert>

    <update id="update" parameterType="com.example.financedashboard.entity.StockInfo">
        UPDATE stock_info
        SET stock_name = #{stockName},
            exchange = #{exchange},
            current_price = #{currentPrice},
            yesterday_close = #{yesterdayClose},
            change_percent = #{changePercent},
            status = #{status},
            last_update_time = #{lastUpdateTime},
            updated_at = #{updatedAt}
        WHERE stock_code = #{stockCode}
    </update>

    <delete id="deleteByStockCode">
        DELETE FROM stock_info WHERE stock_code = #{stockCode}
    </delete>

    <select id="getAllStockCodes" resultType="java.lang.String">
        SELECT stock_code FROM stock_info ORDER BY stock_code ASC
    </select>
    
    <select id="findIdByCode" resultType="java.lang.Long">
        SELECT id FROM stock_info WHERE stock_code = #{stockCode}
    </select>

    <update id="updateStockCompleteInfo">
        UPDATE stock_info
        <set>
            <if test="industry != null">
                industry = #{industry},
            </if>
            <if test="sector != null">
                sector = #{sector},
            </if>
            <if test="totalMarketCap != null">
                total_market_cap = #{totalMarketCap},
            </if>
            <if test="listingDate != null">
                listing_date = #{listingDate},
            </if>
            updated_at = NOW()
        </set>
        WHERE stock_code = #{stockCode}
    </update>
</mapper>